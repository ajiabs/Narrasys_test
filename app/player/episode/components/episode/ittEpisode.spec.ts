
xdescribe('Directive: IttEpisode', () => {

  beforeEach(angular.mock.module('np.client'));

  let $controller;
  beforeEach(angular.mock.inject((_$controller_) => { // tslint:disable-line
    $controller = _$controller_;
  }));

  describe('Episode $scope', () => {
    it('has appState', () => {});
    it('has episode.template', () => {});
    it('has episode.scenes', () => {});
    it('has everything needed for review mode', () => {});
    it('has everything needed for watch mode', () => {});
    it('has everything needed for watch mode', () => {});
    it('has everything needed for discover mode', () => {});
    it('has everything needed for video component', () => {});
    it('has everything needed for windowFg component', () => {});
  });

});
